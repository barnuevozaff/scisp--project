VITE_API_URL=http://localhost:5000/api

# Bonus feature: "Sign in with Google". Use the same Client ID configured
# on the backend (see backend/.env.example for setup instructions).
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
